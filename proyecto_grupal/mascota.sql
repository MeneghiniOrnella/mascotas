CREATE DATABASE createAvatar;

CREATE TABLE 'createAvatar' (
  `id` INT(10) NOT NULL AUTO_INCREMENT,
  `nombre` VARCHAR(30) NULL,
  `genero` VARCHAR(10) NULL,
  'edad' NUMERIC(2) NULL,
  `color` VARCHAR(30) NULL,
  PRIMARY KEY (`id`)) ;

